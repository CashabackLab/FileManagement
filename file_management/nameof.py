import varname

"""
This is a convenient alias for varname's nameof function. We do not claim to
own the varname package nor are we affiliated with its authors. 
Link to varname GitHub: https://github.com/pwwang/python-varname
"""

def nameof(variable):
  """
  Returns given variable name as a string.
  """
  return varname.nameof(variable)
